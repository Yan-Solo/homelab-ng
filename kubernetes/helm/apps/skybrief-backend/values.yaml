app-template:
  defaultPodOptions:
    enableServiceLinks: false

  controllers:
    main:
      containers:
        main:
          image:
            repository: ghcr.io/swatfish/skybrief-backend
            tag: latest
          env:
            ALLOWED_ORIGINS: "https://vcom.dev"
  service:
    main:
      controller: main
      ports:
        http:
          port: 3000
          targetPort: 3000

  persistence:
    # SQLite database and app data
    data:
      enabled: true
      type: persistentVolumeClaim
      accessMode: ReadWriteOnce
      size: 5Gi
      storageClass: local-path-ssd
      globalMounts:
        - path: /app/data
  ingress:
    # Frontend ingress with SSL
    frontend:
      enabled: true
      className: traefik
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt
        traefik.ingress.kubernetes.io/router.entrypoints: websecure
        traefik.ingress.kubernetes.io/router.tls: "true"
      hosts:
        - host: skybrief-backend.intropin.be
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: frontend
                port: 3000
      tls:
        - secretName: skybrief-backend-tls
          hosts:
            - skybrief-backend.intropin.be
