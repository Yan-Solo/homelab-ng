---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-sunnyvale-be
  namespace: default
spec:
  secretName: wildcard-sunnyvale-be-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - "*.sunnyvale.be"
    - "sunnyvale.be"
