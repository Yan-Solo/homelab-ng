---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: home-assistant-external-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: ha.intropin.be 
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: home-assistant-service
            port:
              number: 80
---
apiVersion: v1
kind: Service
metadata:
  name: home-assistant-service
spec:
  type: ExternalName
  externalName: 192.168.0.146
  ports:
  - port: 80
    targetPort: 8123
