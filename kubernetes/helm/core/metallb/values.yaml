metallb:
  controller:
    priorityClassName: system-cluster-critical
  speaker:
    priorityClassName: system-cluster-critical
    # Speaker requires NET_ADMIN capability for ARP functionality
    containerSecurityContext:
      capabilities:
        add:
          - NET_RAW
          - NET_ADMIN
        drop:
          - ALL
      readOnlyRootFilesystem: true
